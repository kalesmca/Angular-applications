import { Injectable } from '@angular/core';
import { BehaviorSubject, Observable } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class TestService {

  constructor() { }

  //   private _donorList: BehaviorSubject = new BehaviorSubject([] );
  //   public readonly donorList: Observable = this._donorList.asObservable();


  // that._sitesFilter.next( that.da
}
